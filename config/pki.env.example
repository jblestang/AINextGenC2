# Production PKI environment variables (coalition exercise)
#
# Lab demos and tests use `PkiMode::Lab` / `run_lab()` APIs — no environment toggle required.

# Comma-separated SPKI PEM files for inbound NMBS verification
MIM_NMB_TRUST=/etc/mim/nmb-trust.pem

# PKCS#8 PEM signing keys (separate NMBS and KAS hierarchies)
MIM_NMB_SIGNING_KEY=/etc/mim/nmb-signing.pk8
MIM_KAS_SIGNING_KEY=/etc/mim/kas-signing.pk8


# Lab / CI: use bundled conformance keys via `PkiMode::Lab`, `run_lab()`, or `HttpExchangeConfig::lab()`.

# FMN LDAP subject directory (fixture-backed in lab mode)
MIM_LDAP_PIP_CONFIG=config/fmn-ldap-pip.toml

# Coalition federation node configuration
MIM_FEDERATION_CONFIG=config/fmn-federation.toml

# Live LDAP (fixture_mode=false in fmn-ldap-pip-ci.toml; CI uses docker-compose.ldap-ci.yml)
# MIM_LDAP_BIND_PASSWORD=secret

# Remote KAS HTTP endpoint (OpenTDF keyAccess stub)
# MIM_KAS_ENDPOINT=https://kas.fmn.mil/v1/rewrap

# SAML/OIDC bearer: Authorization: Bearer <base64url-json-claims> on HTTP requests

# Allied sensor demo: use remote HTTPS replication instead of in-process journal pull
# MIM_FEDERATION_HTTP=1

# Require mTLS client identity for PEP (set true in coalition exercise)
# MIM_REQUIRE_CLIENT_IDENTITY=1
